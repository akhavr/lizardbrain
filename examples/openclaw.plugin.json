{
  "id": "memory-recall",
  "name": "Memory Recall",
  "version": "0.7.0",
  "description": "Auto-injects lizardbrain search results into agent context for community knowledge recall.",
  "entry": "openclaw-plugin.ts",
  "config": {
    "enabled": {
      "type": "boolean",
      "description": "Enable memory recall injection",
      "default": true
    },
    "maxResults": {
      "type": "number",
      "description": "Maximum number of search results to inject",
      "default": 5
    },
    "minMessageLength": {
      "type": "number",
      "description": "Minimum message length to trigger memory search",
      "default": 20
    }
  },
  "dependencies": {
    "lizardbrain": ">=0.7.0"
  }
}
